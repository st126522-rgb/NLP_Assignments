# Prerequisite
* Go inside A1 folder
* First run the jupyter notebook/ a1_final to get the model
* Use those model from app.py to use in the UI

# To run the app
* Go inside `A1/app folder
* Run python app.py
* Go to http://127.0.0.1:8050/ on the browser of your choice